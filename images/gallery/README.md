This folder contains images used by the site photo gallery.

Add your photos here (JPEG/PNG). Filenames will appear in the gallery in sorted order.

Notes:
- Keep images reasonably sized for web (e.g., max 2000px on the long side).
- The gallery page (`/photos/`) looks for files under `/images/gallery/`.
- If you want per-image captions later, we can maintain a small YAML file mapping filenames → captions.
